dependencyResolutionManagement {
    //noinspection GroovyAssignabilityCheck
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "LedDisplayI2cDriver"
include ':LedMatrix'
